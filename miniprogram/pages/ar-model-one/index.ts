var sceneReadyBehavior = require('../../components/common/scene-ready');
// var handleDecodedXML = require('../../components/common/util').handleDecodedXML;

Page({
  behaviors:[sceneReadyBehavior],
  data: {
  }
});

